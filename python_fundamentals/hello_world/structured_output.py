#!/usr/bin/env python3
language = "Python"
version = 3
pi = 3.14159
computation_valid = 2 + 2 == 4

print(
    f"Language: {language} Version: {version} "
    f"Pi approx: {pi:.2f} Computation valid: {computation_valid}"
)
