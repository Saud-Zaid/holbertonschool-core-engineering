# Python Fundamentals - Exception Handling

This project covers handling errors and exceptions in Python using `try: / except:`.

## Files and Tasks

- `safe_print_list.py`: Function that prints `x` elements of a list safely without using `len()`.
